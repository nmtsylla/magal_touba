<% if flash %>
    <div class="container">
      <div class="row">
        <div class="col-md-12">
          <% flash.each do |key, value| %>
              <div class="alert alert-<%= key %>"><%= value %></div>
          <% end %>
        </div>
      </div>
    </div>
<% end %>